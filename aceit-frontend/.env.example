# Copy this file to .env.local for local dev (already defaults to localhost:8000)
# On Vercel: add VITE_API_URL as an environment variable pointing to your Render URL

# Leave blank for local development (defaults to http://localhost:8000)
# Set to your Render backend URL for production:
VITE_API_URL=https://your-aceit-backend.onrender.com
